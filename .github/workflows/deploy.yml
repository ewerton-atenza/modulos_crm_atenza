name: Deploy para Hostinger

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy Catálogo
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4
      
      - name: 🔐 Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
          
      - name: 🚀 Enviar arquivos via SCP
        run: |
          # Envia todos os arquivos necessários para o Docker (Dockerfile, docker-compose, src, etc)
          scp -i ~/.ssh/id_rsa -r ./* ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/home/u937514520/domains/catalog.atenza.digital/public_html/
          
      - name: 🐳 Atualizar Docker na VPS
        run: |
          # Acessa a VPS, entra na pasta e recria o container com a nova build
          ssh -i ~/.ssh/id_rsa ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "cd /home/u937514520/domains/catalog.atenza.digital/public_html/ && docker compose up -d --build"
          
      - name: ✅ Verificar deploy
        run: |
          echo "✅ Deploy e Docker atualizados com sucesso!"
          echo "🌐 URL: https://catalog.atenza.digital"